# 3D 工业监控系统 · 测试清单

> 用途：Chloe 拿到项目后，按这份清单逐项点一遍，确认每个功能正常。
> 场景文件：`Assets/Scenes/SampleScene.unity`
> 后端目录：`IndustrialMonitorAPI/`（独立 .NET 服务，端口 **5175**）

---

## 0. 字体问题已修复（本次改动）

**现象**：部分中文/符号显示不完整、缺字、变成方块。
**根因**：你的 `Assets/Fonts/Arial Unicode SDF.asset` 虽然指向你导入的 Arial Unicode，但图集是**静态单张 1024×1024、只烤了 122 个字符**，中文字根本没进图集。

**本次已做**：把这份字体资源改成 **动态图集 + 开启多图集**（`m_IsMultiAtlasTexturesEnabled = 1`）。
- 现在运行时 TMP 会从 `Arial Unicode.ttf` 按需把任意中文字形补进图集，全部中文都能正常显示。
- 源字体指向正确（已校验 GUID 指向 `Arial Unicode.ttf`）。

**你要做的（二选一）**：
1. 直接进 Play 模式即可看到完整中文（推荐，零操作）。
2. 若想在编辑器静止态也 100% 所见即所得，跑一次【完整静态烘焙】见文末附录 A。

---

## 1. 跑起来前的准备

- [ ] **装配 UI（只需一次，已装配过可跳过）**
  - Unity 顶部菜单：`Tools / Setup Industrial Monitor UI V1` → 弹出完成框 → 按 **Cmd/Ctrl+S** 保存场景。
  - 再跑：`Tools / 一键装配：多车间与登录UI` → 完成后同样 **Cmd/Ctrl+S** 保存。
- [ ] **启动后端 API**
  - 需要 .NET 10 SDK（`IndustrialMonitorAPI.csproj` 目标 `net10.0`）。
  - 在 `IndustrialMonitorAPI/` 目录执行：`dotnet run`（或 `dotnet watch run`）。
  - 看到监听 `http://localhost:5175` 即成功。首次会自动建库+迁移+种子数据。
- [ ] **Unity 连后端端口对齐（重要坑）**
  - 前端 `ApiClient` 默认地址是 `http://localhost:5000`，但后端实际在 **5175**。
  - 进入游戏 → 系统设置页 → 把「API 地址」改成 `http://localhost:5175` → 点「测试连接」应显示●已连接（绿）。

---

## 2. 功能测试清单（进 Play 模式逐项勾）

### A. 顶部状态栏 / 导航
- [ ] 顶部「系统状态」「API 状态」有文字且进入 Play 后有联动（不再写死）。
- [ ] 底部导航 5 个按钮可切换：设备列表 / 实时监控 / 报警记录 / 历史记录 / 系统设置。

### B. 概览面板（Overview）
- [ ] 显示正常设备数、故障数、**离线数**（新增项）。
- [ ] 显示「车间统计：」分组统计（一号车间 / 二号车间 设备分布）。

### C. 实时监控 + 设备详情
- [ ] 点 3D 场景里的设备（Device A/B/C）→ 弹出设备详情面板，温度/压力数值在跳动。
- [ ] 设备详情有「离线 / 上线」按钮，点击后概览「离线」计数随之变化。
- [ ] 设备详情有「恢复正常」按钮。

### D. 报警阈值自动化（重点）
- [ ] 在设备详情点「模拟温度故障」→ 温度冲到 ≥90℃ → 触发**温度故障**报警，底部「最近报警」栏变红并显示内容。
- [ ] 点「模拟压力故障」→ 压力到 ≥2.3 → 触发**压力故障**报警。
- [ ] 阈值参考（可在 DeviceController 改）：温度预警 78℃ / 故障 90℃；压力预警 2.0 / 故障 2.3。
- [ ] 报警解除后，底部栏恢复「✓ 当前无异常报警」（绿）。

### E. 历史数据页（折线图）
- [ ] 切到「历史记录」页：默认显示本地采样曲线（每 2s 采一次）。
- [ ] 设备切换：设备 A / B / C 三个按钮。
- [ ] 指标切换：温度 / 压力 两个按钮。
- [ ] 车间切换：一号车间 / 二号车间 两个按钮（多车间扩展）。
- [ ] 等约 10s 后，前端把本地缓冲**批量上报后端**（POST /api/history）；切到后端长期数据后折线应衔接。
- [ ] 页面底部「统计信息」文字更新（如 暂无数据/点数/均值）。

### F. 多车间（二号车间）
- [ ] 场景中应出现第二套外壳 + 设备 C（物理分区，X 偏移 +40）。
- [ ] 概览/历史的车间统计与切换按 A/B→一号车间、C→二号车间 正确归类。

### G. 登录 + 操作日志
- [ ] 系统设置页有登录区：用户名 / 密码 / 登录 / 当前用户 / 登录状态。
- [ ] 用 `admin / admin123`（或 `operator / operator123`）登录 → 「当前用户：admin」「登录状态」变绿。
- [ ] 登录后做任何操作（测试连接、离线设备、切页等）→ 后端应写入操作日志。
- [ ] 未登录调用 `POST /api/logs` 应返回 **401**（见第 3 节 curl 验证）。

### H. 报表导出
- [ ] 系统设置页「导出报警 CSV」「导出设备 CSV」按钮可点击并生成文件（默认导出到应用持久化目录）。

---

## 3. 后端 API 直测（终端 curl，验证服务端）

后端根：`http://localhost:5175`

```bash
# 1) 健康检查 / 设备列表
curl http://localhost:5175/api/devices

# 2) 按车间过滤（A/B 一号车间，C 二号车间）
curl http://localhost:5175/api/devices/workshop/一号车间

# 3) 登录拿 token
TOKEN=$(curl -s -X POST http://localhost:5175/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}' | python3 -c "import sys,json;print(json.load(sys.stdin)['data']['token'])")
echo "TOKEN=$TOKEN"

# 4) 带 token 写一条操作日志（应 200）
curl -X POST http://localhost:5175/api/logs \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"action":"测试","target":"API","detail":"curl 验证"}'

# 5) 不带 token 写日志（应 401）
curl -X POST http://localhost:5175/api/logs \
  -H "Content-Type: application/json" \
  -d '{"action":"x","target":"y","detail":"z"}'

# 6) 历史数据写入后再查
curl "http://localhost:5175/api/history?deviceId=A&metric=temperature&minutes=60"
```

**预期**：1/2/3/4 正常返回；第 5 步返回 401。

---

## 4. 已知坑 / 提醒

1. **端口不一致**：前端默认 `localhost:5000`，后端实际 `5175`。务必在设置页改地址，否则「测试连接」一直红。
2. **改完场景要保存**：两个 `Tools` 菜单装配后都要 **Cmd/Ctrl+S**，否则下次打开丢失。
3. **演示账号是明文内存态**：`admin/admin123`、`operator/operator123` 仅本地 Demo，未接数据库/哈希，上生产前需替换。
4. **字体**：已开动态多图集，进 Play 即完整；静止编辑态若仍见缺字，按附录 A 烘焙一次即可。
5. **数据库文件**：`IndustrialMonitorAPI/industrial_monitor.db`（SQLite），已种子化；重来可删库让迁移重建。

---

## 附录 A：完整静态烘焙（可选，编辑器静止态也全字显示）

适用于想彻底摆脱运行时动态补字、或出包到真机/IL2CPP 时更稳：

1. Unity 菜单 `Window / TextMeshPro / Font Asset Creator`。
2. Source Font：选 `Assets/Fonts/Arial Unicode.ttf`。
3. Character Set：`Custom Characters`，把 `Assets/Fonts/arial_unicode_chars.txt`（已为你生成，含项目全部 719 个字符）内容粘入；或选 `Characters from File` 直接加载该 txt。
4. Atlas Resolution：`4096 x 4096`（719 字最稳）；Sampling Point Size：`48`；Render Mode：`SDFAA`。
5. 点 `Generate Font Atlas`，完成后 **Save** 时**覆盖保存到同名** `Assets/Fonts/Arial Unicode SDF.asset`（保留 GUID，场景 145 处引用不丢）。
6. 之后 `m_IsMultiAtlasTexturesEnabled` 可保持开启作为兜底。

---

## 附录 B：本次改动文件

- `Assets/Fonts/Arial Unicode SDF.asset` — 开启多图集（修复缺字）。
- `Assets/Fonts/arial_unicode_chars.txt` — 项目用到的全部字符清单（供烘焙输入）。
- `测试清单.md` — 本文件。

> 字体改动是本地资源修改，未提交远端。确认正常后可 `git add` 这几个文件并提交。
